# 安全与隐私

open-hemory 是个人设备之间的实验性录音工具。默认只在可信局域网使用，云服务关闭；公开源码不意味着用户数据可以公开。

## 使用边界

- 仅录制自己有权录制的内容，遵守适用法律与参与者的知情/同意要求。打开 Watch App 后可能开始录音，请确认录音状态。
- 接收器仅供可信局域网设备使用。不要转发路由器端口、建立公网隧道或公开本机看板。接收器使用 TLS、Bearer token 和配对证书指纹；不要通过跳过 TLS 验证解决连接问题。
- 使用强随机令牌并保护 Mac 账号。配对 JSON 同时包含服务器地址、令牌和证书指纹，应当作凭据保管。只发给自己受信任的设备。
- Watch/iPhone 使用 Keychain 保存配对信息；Mac 原音、数据库和配置主要依靠本地文件权限保护，应用没有提供额外静态加密。审慎配置云盘、Time Machine 和 FileVault。
- 本地模型安装下载公开依赖与权重。可选云摘要会发送文字，旧云 STT 会发送音频；开启前自行确认供应商数据政策、价格与额度。仓库不含调用密钥，也不默认启动云评测。

## 什么不应进入仓库或 Issue

不要提交 `private/`、`runtime/`、`.validation/`、录音、录音元数据、真实转写、原始模型回包、SQLite 数据库、服务日志、模型权重、`.env`、真实配对、证书/私钥、签名证书、provisioning profile、Xcode 用户目录或个人验收记录。测试只应使用明确合成的输入。提交前检查暂存区和文件内容；即使配置了 `.gitignore`，已跟踪文件仍可能被提交。

`examples/` 和 `watch/pairing.example.json` 只允许无效占位值。生成器输出的公开 Xcode 工程应使用示例 Bundle ID、空 Development Team，不携带个人签名配置。

## 发现问题或凭据暴露

请通过仓库维护者公开列出的私密联系方式，或在仓库启用后使用 GitHub Private Vulnerability Reporting 报告安全问题。不要把录音、配对凭据、私钥或完整日志发到公开 Issue。维护者尚未公布私密渠道时，可公开询问联系方式，但不要附带可利用细节或敏感数据。此项目没有承诺响应时限。

凭据一旦公开，应立即停止相关接收服务、轮换令牌；私钥暴露时重新生成证书，并在自己的设备重新配对。只删除工作目录里的文件不能撤回已经进入 Git 历史、缓存、fork 或下载副本的数据。向云服务商单独撤销已暴露的 API key。

## 支持状态

当前为源码实验版本，尚无长期支持或完整安全审计承诺。测试覆盖部分校验、鉴权、去重与访问约束，不能证明没有漏洞。不要将其作为唯一的关键记录、医疗记录或合规留存系统。
